#02/08/2025 -> Ejercicios que devuelve los índices de un array que sumen un número
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        
        for i in range(len(nums)):
            first = nums[i]
            for j in range(len(nums)):
                second = nums[j]
                if i != j and (first + second == target):
                    return [i,j]
